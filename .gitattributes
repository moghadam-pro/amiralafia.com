# Keep source files LF in the working tree on every platform, so the theme zip
# built by tools/build-theme.py is byte-identical regardless of who builds it.
* text=auto eol=lf

*.png  binary
*.jpg  binary
*.woff2 binary
*.zip  binary
